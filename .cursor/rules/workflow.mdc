---
alwaysApply: true
---

## 三阶段工作流程（必须严格遵守）

### 第一阶段：需求分析与方案规划

**执行步骤**：
1. 仔细分析需求，深入理解用户提出的问题
2. 根据整体项目情况进行全面分析
3. 判断缺少哪些参考信息
4. 优先提出所需要补充的信息内容

**输出内容**：
- 需要补充的信息清单
- 准备执行的方案大致方向（如果提出多个方案，需明确标注方案编号，如"方案1"、"方案2"等）
- 可能遇到的挑战和注意事项

**多方案处理要求**：
- 如果提出了多个方案，必须等待用户明确指出使用哪个方案（如"使用方案1"、"采用方案2"等）才能进入第二阶段
- 在用户明确选择方案之前，应停留在第一阶段，等待用户指示

**重要说明**：
- 用户会在这个阶段补充信息、确定大致执行方向
- 此阶段不进行任何文件修改或生成操作
- **信息补充后的处理**：如果用户只是返回了需要补充的信息，而未要求直接进入下一阶段，应根据用户补充的信息重新在第一阶段输出分析结果，而不是自动进入第二阶段（重要！！！！）
- **多方案选择**：如果提出了多个方案，必须等待用户明确选择使用哪个方案后才能进入第二阶段

### 第二阶段：方案细化

**执行步骤**：
1. 根据第一阶段确定的大致方案，细化执行步骤
2. 细化到具体文件修改内容
3. 列出所有需要创建、修改、删除的文件清单
4. 说明每个步骤的具体操作

**输出内容**：
- 详细的执行步骤清单
- 具体文件修改内容说明
- 文件路径和操作类型（创建/修改/删除）

**重要约束**：
- ⚠️ **禁止在此阶段直接修改或生成任何实质性的文件**
- ⚠️ **只输出方案，不执行操作**

**迭代机制**：
- ✅ **用户可以在本阶段对方案进行干涉和修改**
- ✅ **用户会审查细化方案，并提出修改意见**
- ✅ **需要根据用户的反馈调整方案，直到用户满意为止**
- ✅ **只有在用户明确表示满意并确认进入下一阶段后，才能进入第三阶段**
- ✅ **用户在第二阶段添加新需求时的处理**：如果用户在第二阶段添加新的需求，需要根据当前情况分析：
  - 如果新需求可以在当前方案基础上调整，则修改当前细化方案
  - 如果新需求涉及重大变更或与当前方案冲突，应返回第一阶段重新进行需求分析与方案规划
  - 必须等待用户明确指示（如"确认进入下一阶段"、"可以执行"等）后才能进入第三阶段

### 第三阶段：方案执行

**执行步骤**：
1. 根据第二阶段给出的细化方案执行
2. 按照步骤清单逐一完成文件操作
3. 根据用户接受结果记录或修改当前项目情况
4. 更新项目状态记录

**输出内容**：
- 执行结果报告
- 已完成的操作清单
- 项目状态更新（如有）
- **所有修改文档的清单**：在第三阶段结束时，必须列出所有涉及修改的文档，并提供可点击跳转的链接（使用 Markdown 链接格式：`[文档名称](Docs/文档名称.md)` 或 `[文档名称](相对路径)`）

**重要说明**：
- 如果用户不需要执行，可跳过此步
- 必须在用户明确确认后才能进入此阶段
- **阶段自动结束机制**：第三阶段输出执行结果后自动结束该阶段
- **重新开始机制**：第三阶段结束后，用户继续追问或反馈执行结果时，必须从第一阶段重新开始整个工作流程

### 流程约束（必须遵守）

- ❌ **不允许自行跳过阶段**
- ❌ **不允许连续执行多个阶段**
- ✅ **每个阶段结束后必须等待用户确认**
- ✅ **用户可以在任意阶段结束回答**
- ✅ **阶段切换必须由用户明确指示**：只有在用户明确说出"确认进入下一阶段"、"可以执行下一阶段"等类似明确指示时，才能切换到下一阶段；否则应继续在当前阶段工作
- ✅ **必须在当前阶段完成所有必要工作后再等待用户指示**
